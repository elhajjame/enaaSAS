//Create a function multiplicationTable(n) that returns an array containing the multiplication table of n.
//Example: for n = 3 → [3, 6, 9, ..., 30].

function multiTable(n) {
    let arr = [];

    for (let i = 1; i <= 10; i++) {
        arr.push(n * i); 
    }

    return table;
}

console.log(multiTable(3));