const prompt = require ('prompt-sync')();

console.log("Sum and Maximum of a Series")
console.log("\n");

let sum = 0;
let max = 0;
let num;

while(num !== 0)
{
     let num = Number(prompt("Enter a positive number less than 100, press 0 to stop: "));
     break;

     
}